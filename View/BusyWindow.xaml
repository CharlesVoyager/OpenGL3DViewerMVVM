<UserControl x:Class="View3D.view.BusyWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Background="Transparent"
             IsVisibleChanged="OnVisibilityChanged">
    <Grid>
        <Grid MinWidth="300" MinHeight="120" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Rectangle Stroke="Black" Fill="#FFF0F0F0" RadiusX="10" RadiusY="10" Grid.RowSpan="4" />

            <TextBlock Text="Loading a model..." HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10" Width="265" FontFamily="Arial" FontWeight="Bold" FontSize="26" TextAlignment="Center" />
            <ProgressBar Grid.Row="2" Height="32" Margin="20,0" IsIndeterminate="False" Value="{Binding LoadModelProgress}"/>
            <TextBlock Name="labelElapsedTime" Grid.Row="3" Text="Elapsed Time" FontFamily="Arial" HorizontalAlignment="Left" Margin="20,10,0,8" VerticalAlignment="Center"/>
            <TextBlock Name="textBlock_time" Grid.Row="3" HorizontalAlignment="Right" Margin="0,10,20,8" Text="00:00:00" VerticalAlignment="Top" TextAlignment="Right"/>
            <Button Grid.Row="2" Content="X" FontFamily="Arial" HorizontalAlignment="Right" Margin="0,0,25,0" VerticalAlignment="Center" Background="{x:Null}" FontWeight="Bold" FontSize="16" BorderBrush="{x:Null}" Click="buttonCancel_Click" />
        </Grid>
    </Grid>
</UserControl>
