<Window x:Class="View3D.view.STLComposer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="540" 
             Width="400"
             Title="STLCompoer"
             ResizeMode="NoResize" Closing="Window_Closing">

    <Window.Resources>
        <!-- Validation error style for TextBox -->
        <Style x:Key="ValidatedTextBox" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="180"/> <!-- List -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DataGrid
            ItemsSource="{Binding Models}"
            SelectedItem="{Binding SelectedModel}"
            CanUserAddRows="False" CanUserDeleteRows="False"
            AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Trangles" Binding="{Binding Triangles}" Width="80"/>
                <DataGridTextColumn Header="Outside" Binding="{Binding Outside}" Width="60"/>
            </DataGrid.Columns>

        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <!-- ═══════════════════════════════════════════
                 panel1  –  Trans / Scale / Rotate controls
                 ═══════════════════════════════════════════ -->
            <Border Height="107" BorderBrush="Gray" BorderThickness="0.5" CornerRadius="3" Padding="10" Margin="4,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>   <!-- row labels -->
                        <ColumnDefinition Width="Auto"/> <!-- X label -->
                        <ColumnDefinition Width="68"/>   <!-- X value -->
                        <ColumnDefinition Width="Auto"/> <!-- Y label -->
                        <ColumnDefinition Width="68"/>   <!-- Y value -->
                        <ColumnDefinition Width="Auto"/> <!-- Z label -->
                        <ColumnDefinition Width="68"/>   <!-- Z value -->
                        <ColumnDefinition Width="*"/>    <!-- LockAspect -->
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                    </Grid.RowDefinitions>

                    <!-- Row labels -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Trans" VerticalAlignment="Center" Margin="4,0"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Scale" VerticalAlignment="Center" Margin="4,0"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Rotate" VerticalAlignment="Center" Margin="4,0"/>

                    <!-- Trans -->
                    <TextBox x:Name="textTransX" Grid.Row="0" Grid.Column="2"
                             Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             Text="{Binding SelectedItem.PositionX, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBox x:Name="textTransY" Grid.Row="0" Grid.Column="4"
                             Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             Text="{Binding SelectedItem.PositionY, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBox x:Name="textTransZ" Grid.Row="0" Grid.Column="6"
                             Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             Text="{Binding SelectedItem.PositionZ, UpdateSourceTrigger=PropertyChanged}"/>
                    <!-- Scale -->
                    <TextBox x:Name="textScaleX" Grid.Row="1" Grid.Column="2" Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             TextChanged="textScaleX_TextChanged"/>
                    <TextBox x:Name="textScaleY" Grid.Row="1" Grid.Column="4" Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             TextChanged="textScaleY_TextChanged"/>
                    <TextBox x:Name="textScaleZ" Grid.Row="1" Grid.Column="6" Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             TextChanged="textScaleZ_TextChanged"/>

                    <!-- Rotate -->
                    <TextBox x:Name="textRotX" Grid.Row="2" Grid.Column="2"
                             Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             TextChanged="textRotX_TextChanged"/>
                    <TextBox x:Name="textRotY" Grid.Row="2" Grid.Column="4"
                             Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             TextChanged="textRotY_TextChanged"/>
                    <TextBox x:Name="textRotZ" Grid.Row="2" Grid.Column="6"
                             Margin="4,4" VerticalAlignment="Center"
                             Style="{StaticResource ValidatedTextBox}"
                             TextChanged="textRotZ_TextChanged"/>
                 </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════
                 panelAnalysis  –  Object Analysis GroupBox
                 ═══════════════════════════════════════════ -->
            <Border x:Name="panelAnalysis" Height="200" Visibility="Visible">
                <GroupBox Header="Object Analysis" Margin="4,2" Background="White" x:Name="groupBoxObjectAnalysis">
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="45*"/>
                            <ColumnDefinition Width="35*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="24"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0 – Original Model Size -->
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Original Model Size (X, Y, Z):"
                                   VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtOriginalModelSize"
                                   Grid.Row="0" Grid.Column="1"
                                   Text="(0, 0, 0)" VerticalAlignment="Center"/>

                        <!-- Row 1 – Loop edges -->
                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Text="Loop edges:" VerticalAlignment="Center"/>
                        <TextBlock x:Name="labelLoopEdges"
                                   Grid.Row="1" Grid.Column="1"
                                   Text="0" VerticalAlignment="Center"/>

                        <!-- Row 2 – Highly connected -->
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Highly connected edges:"
                                   VerticalAlignment="Center"/>
                        <TextBlock x:Name="labelHighConnected"
                                   Grid.Row="2" Grid.Column="1"
                                   Text="0" VerticalAlignment="Center"/>

                        <!-- Row 3 – Vertices -->
                        <TextBlock Grid.Row="3" Grid.Column="0"
                                   Text="Vertices:" VerticalAlignment="Center"/>
                        <TextBlock x:Name="labelVertices"
                                   Grid.Row="3" Grid.Column="1"
                                   Text="0" VerticalAlignment="Center"/>

                        <!-- Row 4 – Edges -->
                        <TextBlock Grid.Row="4" Grid.Column="0"
                                   Text="Edges:" VerticalAlignment="Center"/>
                        <TextBlock x:Name="labelEdges"
                                   Grid.Row="4" Grid.Column="1"
                                   Text="0" VerticalAlignment="Center"/>

                        <!-- Row 5 – Triangles -->
                        <TextBlock Grid.Row="5" Grid.Column="0"
                                   Text="Triangles:" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtTriangles"
                                   Grid.Row="5" Grid.Column="1"
                                   Text="0" VerticalAlignment="Center"/>

                        <!-- Row 6 – Debug Info -->
                        <TextBlock Grid.Row="6" Grid.Column="0"
                                   Text="Debug Info:" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtDebugInfo"
                                   Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Margin="80,0,0,0"
                                   Text="" VerticalAlignment="Center"/>
                    </Grid>
                </GroupBox>
            </Border>
            
        </StackPanel>
    </Grid>
</Window>
