<UserControl x:Class="View3D.view.BusyWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="#9B000000">

    <UserControl.Resources>
        <Style x:Key="busyWindowBorderStyle" TargetType="Border">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedModel}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource busyWindowBorderStyle}" Visibility="Visible">

        <Grid MinWidth="289" MinHeight="140" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Rectangle Stroke="Black" Fill="#FFF0F0F0" RadiusX="10" RadiusY="10" Grid.RowSpan="4" />
            <ProgressBar Name="busyProgressbar" Height="32" Margin="20,0" IsIndeterminate="True" Grid.Row="2" />
            <TextBlock Text="Loading a model..." HorizontalAlignment="Center" Margin="0,10" VerticalAlignment="Center" Width="265" FontFamily="Arial" FontWeight="Bold" DataContext="{Binding}" FontSize="26" TextAlignment="Center" />
            <TextBlock Name="labelElapsedTime" Text="Elapsed Time" FontFamily="Arial" HorizontalAlignment="Left" Margin="20,10,0,8" VerticalAlignment="Center" Grid.Row="3" />
            <TextBlock Name="textBlock_time" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" Margin="0,10,20,8" Text="00:00:00" VerticalAlignment="Top" TextAlignment="Right"/>
            <Button x:Name="buttonCancel" Grid.Row="2" Grid.ColumnSpan="2" Content="X" FontFamily="Arial" HorizontalAlignment="Right" Margin="0,0,25,0" VerticalAlignment="Center" Background="{x:Null}" FontWeight="Bold" FontSize="16" BorderBrush="{x:Null}" Click="buttonCancel_Click" />
        </Grid>

    </Border>
</UserControl>
